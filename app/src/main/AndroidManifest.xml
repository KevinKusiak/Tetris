<?xml version="1.0" encoding="UTF-8"?>

    <manifest package="com.example.tetris" xmlns:android="http://schemas.android.com/apk/res/android">


    -<application android:theme="@style/AppTheme" android:screenOrientation="portrait" android:supportsRtl="true" android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true">

    <activity android:name=".Marathon"/>

    <activity android:name=".Maps"/>

    <activity android:name=".Settings"/>

    <activity android:name=".Gamemodes"/>


    <activity android:name=".StartScreen">


    <intent-filter>

    <action android:name="android.intent.action.MAIN"/>

    <category android:name="android.intent.category.LAUNCHER"/>

</intent-filter>

</activity>

</application>

</manifest>